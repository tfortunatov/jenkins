/* 
   This Jenkinsfile file should be present in the root of each branch that should
   be managed by multibranch trigger.
   Multibranch trigger is being used in order to track branches that should be
   built manually using original CI pipeline
*/

// Add node
node ('master'){
/* 
   Add stages. In future we may add pipeline stage dependency feature to get 
   pipeline visualisation.
*/
// stage 'Build and Test'
}
